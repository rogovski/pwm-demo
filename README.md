# About

cool button with react-native. works on web and (should) work on android and ios

# Usage

install expo globally

```
npm install -g expo
```

clone and build

```
cd pwm-demo
npm install
npm run web
```
